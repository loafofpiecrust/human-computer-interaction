import { css } from "@emotion/react"

export const row = css({
  display: "flex",
  flexFlow: "row",
})

export const box = css({
  border: "3px solid black",
  width: "100%",
  minHeight: "200px",
})
